#ifndef IOC_H
#define IOC_H

// Interface para análise de Índice de Coincidência
float index_of_coincidence(const char *text, int start, int step);
int alternative_key_length_method(const char *ciphertext);

#endif